# researchcompany — 로담한의원 리서치 전문 워크스페이스

이 저장소는 **로담한의원**(흉터 전문 한의원, `_context/brand.md` 참고)의 키워드/시장/경쟁사 리서치를
전문적으로 수행하는 작업 공간이다. 코드 프로젝트가 아니라 리서치 산출물(마크다운 보고서, HTML 리포트)을
쌓아가는 곳이므로, 일반적인 소프트웨어 엔지니어링 관례보다 아래의 리서치 워크플로 규칙을 우선한다.

## 디렉터리 구조

- `_context/brand.md` — 로담한의원 브랜드 팩트의 **단일 진실 공급원(SSOT)**. 지점 수, 연혁, 수치,
  자사 서비스 등은 반드시 이 파일 기준으로 인용하고, 확인되지 않은 성과는 절대 지어내지 않는다(픽션 금지).
  새 사실을 확인하면 먼저 이 파일을 업데이트한 뒤 사용한다.
- `.claude/skills/keyword-market-research/` — 키워드 하나를 네이버(블로그/카페/지식iN/지역검색/데이터랩/
  검색광고 절대검색량)와 구글에서 조사해 로담 브랜드 노출 현황과 공백을 분석하는 핵심 스킬. "키워드 조사",
  "시장조사" 요청 시 이 스킬(`keyword-market-research`)을 사용한다.
- `scripts/naver_searchad.mjs` — 네이버 검색광고 API로 키워드 절대 월간 검색수(PC/모바일)를 조회하는
  스크립트. naver-search-mcp가 지원하지 않는 기능이라 직접 호출한다. 사용법:
  `node scripts/naver_searchad.mjs 키워드1 키워드2 ...`. 자격증명은 `.env.searchad`(gitignore 대상,
  절대 커밋 금지)에서 로드.
- `outputs/` — 키워드별 조사 결과. 파일명 규칙: `{키워드}_{YYYYMMDD}.md` (필요 시 `.html`도 함께 생성).
- `.mcp.json` — `naver-search` MCP 서버 설정 (네이버 오픈API 클라이언트 자격증명 포함 — 커밋 시 주의).

## 리서치 원칙

- **숫자는 반올림·가공 금지, 있는 그대로 인용.** 출처가 불명확한 수치는 쓰지 않는다.
- **경쟁사 조사 결과는 누적한다.** 새 키워드 조사에서 반복 등장하는 경쟁 한의원/피부과/성형외과가 있으면
  기존에 확인된 경쟁사인지 먼저 확인하고, 신규면 트래커에 추가한다(경쟁사 트래커는 auto-memory에
  `project_competitor-tracker.md`로 관리되며, 최신 상태는 그쪽을 신뢰할 것).
- **네이버 검색광고 API 주의사항**: `hintKeywords`에 공백이 포함된 키워드를 넣으면 HTTP 400 오류가 난다.
  공백을 제거한 형태로 재시도할 것 (예: "편평사마귀 치료법" → "편평사마귀치료법").
- 신규 진출 서비스(예: 쥐젖제거·편평사마귀·지루각화증 레이저 치료)를 "로담과 무관한 카테고리"로 취급하지
  말고, 신규 진출 시장의 경쟁 구도 분석으로 프레이밍한다.

## 작업 시 유의사항

- 이 저장소는 git 저장소가 아니다(초기화되어 있지 않음). 버전 관리가 필요하면 사용자에게 먼저 확인한다.
- `_templates/`에는 스크린샷 등 대용량 바이너리가 들어있을 수 있으니 불필요하게 읽지 않는다.
- 산출물은 한국어로 작성한다 (브랜드와 타겟 고객이 한국어 사용자).
